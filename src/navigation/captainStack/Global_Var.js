import {Window} from "../themes/Window"

const headerColor="#15756c"
const headerTextColor="white"
const iconSize=24
const iconfocuscolor="white"
const iconunfocuscolor="white"
const windowWidth = Window.width;
const windowHeight = Window.height;
 

 const GV = {
 headerColor,
 headerTextColor,
 iconSize,iconfocuscolor,iconunfocuscolor,
 windowHeight,windowWidth
 }

   export default   GV;